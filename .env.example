# ==============================================================================
# Carely Frontend — Environment Variables
# Copy this file to .env and fill in the actual values.
# NEVER commit the real .env file to git.
# ==============================================================================

# The base URL of the Carely backend API (no trailing slash, no /api suffix)
# Local dev: http://localhost:5000
# Production (Render): https://your-carely-backend.onrender.com
VITE_API_URL=http://localhost:5000
